name: Build AutoClicker

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Build
      run: dotnet build AutoClicker/AutoClicker.csproj --configuration Release
    - name: Publish Self-Contained
      run: dotnet publish AutoClicker/AutoClicker.csproj --configuration Release --self-contained true --runtime win-x64
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: AutoClicker
        path: AutoClicker/bin/Release/net6.0-windows/win-x64/publish/
